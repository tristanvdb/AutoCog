// Test return statements with labels

record Data {
  is text;
}

prompt processor {
  is {
    input is Data;
    intermediate is text;
    result is text;
  }
  channel {
    input get input;
  }
  return {
    success is "processing complete";
    intermediate use intermediate;
    use result;
  }
}

export processor as main;
