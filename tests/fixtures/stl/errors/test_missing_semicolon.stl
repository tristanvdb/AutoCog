// Error: missing semicolon after field
// expect-error: Expected token `';'` but found `identifier`
prompt broken {
  is {
    name is text<length=10>
    age is text<length=5>;
  }
}
export broken as main;
