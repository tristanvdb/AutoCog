// Error: empty variable name in a format string
// expect-error: Empty variable name in format string
prompt p { is { f is text<length=4>; } return { use f; } annotate { f as f"hello {}"; } }
export p as main;
