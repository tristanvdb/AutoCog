// Error: __syntax__ is reserved (catalog lands later).
// expect-error: system namespace '__syntax__' is reserved but not yet available
prompt main {
  is { q is text<length=4>; }
  search { text.beams is __syntax__.indent; }
  channel { q get q; }
  return { use q; }
}
