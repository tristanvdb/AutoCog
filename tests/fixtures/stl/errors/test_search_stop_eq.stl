// Error: equality is not a termination comparison.
// expect-error: operator not allowed in a termination predicate
prompt main {
  is { q is text<length=4>; }
  search { queue.stop is (__status__.tree.terminals == 5); }
  channel { q get q; }
  return { use q; }
}
