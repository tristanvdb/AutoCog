// Error: an annotate path that names no field was silently dropped.
// expect-error: annotate path 'nonexistent' does not name a field
prompt main {
  is { q is text<length=4>; }
  channel { q get q; }
  return { use q; }
  annotate { nonexistent as "does not exist"; }
}
